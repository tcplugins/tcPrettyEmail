#if ($info.hasBranch())
Build $info.projectName :: $info.buildTypeName (branch: ${info.formattedBranchName}) #$info.buildNumber failed ($info.statusLowercase)
#else
Build $info.projectName :: $info.buildTypeName #$info.buildNumber failed ($info.statusLowercase)
#end