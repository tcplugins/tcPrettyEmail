#if ($info.hasBranch())
✅ Build $info.projectName :: $info.buildTypeName (branch: ${info.formattedBranchName}) #$info.buildNumber successful ($info.statusLowercase)
#else
✅ Build $info.projectName :: $info.buildTypeName #$info.buildNumber successful ($info.statusLowercase)
#end